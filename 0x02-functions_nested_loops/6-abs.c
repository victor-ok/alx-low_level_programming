#include "main.h"

/**
 * main - main block
 * Description:  a function that computes the absolute value of an integer.
 * Return 0
*/

int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
	return (n);
}
